# The Zone ID is a unique identifier for your Cloudflare zone (such as a specific website or domain).
# It is needed to ensure that API operations are performed on the correct zone.
CLOUDFLARE_API_TOKEN={YOUR_KEY}
CLOUDFLARE_ZONE_ID={YOUR_ZONE_ID}

